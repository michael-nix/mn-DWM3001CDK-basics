CONFIG_SPI=y

# Logging
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3

# Console / printk over UART (uart0 is chosen in the board DTS)
CONFIG_PRINTK=y

# The Qorvo driver pulls in printf-style formatting and needs a decent stack
CONFIG_MAIN_STACK_SIZE=4096
CONFIG_CBPRINTF_FP_SUPPORT=y
